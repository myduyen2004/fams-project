# Production Environment - No hardcoded API URL so Vercel injected variables take precedence
# Set VITE_API_URL and (optionally) VITE_WS_URL in Vercel Environment Variables
# IMPORTANT: Set a strong random key in Vercel dashboard for production
VITE_GRADE_ENCRYPTION_KEY=${VITE_GRADE_ENCRYPTION_KEY}
